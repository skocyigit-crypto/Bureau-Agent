---
name: User terminology quirks
description: Non-obvious words the owner uses that map to specific app features.
---

# User terminology (owner skocyigit-crypto)

- **"Canvas"** = the live **talking AI avatar** (`@workspace/ai-avatar`: `TalkingAvatar` /
  `AvatarDock` — animated SVG face + on-device TTS). It is NOT the Replit design-canvas
  board and NOT the bottom-right AI assistant chat button.
  **Why:** the owner asked to make "Canvas" always live on the home page and draggable;
  on clarification they confirmed it meant the talking avatar. Future "Canvas" requests
  almost certainly concern this avatar widget, not the workspace canvas.
  **How to apply:** when the owner says "Canvas", reach for the avatar feature
  (web: floating widget on the dashboard; also embedded in Asistan / Commandant / Gmail-agent
  pages). Confirm only if context strongly suggests otherwise.
